import { Router } from 'express';import { requireAuth } from '../middleware/auth.js';import { query } from '../database/pool.js';
const r=Router().use(requireAuth);r.get('/',async(req,res)=>{const q=await query(`select id,name,type,os,status,permissions,last_seen_at,ip_address,created_at from devices where user_id=$1 order by last_seen_at desc`,[req.user.id]);res.json({success:true,devices:q.rows});});r.delete('/:id',async(req,res)=>{const q=await query('delete from devices where id=$1 and user_id=$2 returning id',[req.params.id,req.user.id]);res.status(q.rowCount?200:404).json(q.rowCount?{success:true}:{success:false,error:'Device not found'});});export default r;
